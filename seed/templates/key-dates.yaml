title: Key Dates
shell: document
pageType: component
components:
  - type: section
    id: dates
    eyebrow: Calendar
    heading: Key dates
    components:
      - type: definition_list
        items:
          - term: Replace with an event name
            definition: Its date
          - term: Second event
            definition: Its date
  - type: callout
    variant: info
    title: This is a shared component page.
    body: "Embed this list on any page with a `type: ref` block pointing at this page's slug, instead of repeating dates across a plan, a deal page, and a status update. Update the dates here and every page that embeds it shows the change once this page is marked trusted."
