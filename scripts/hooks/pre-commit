#!/usr/bin/env bash
set -euo pipefail

# kazam-generated build artifacts (kazam.css, generated/kazam*, agents
# reference) are gitignored — nothing to guard against here anymore, since
# they can no longer be staged at all.

echo "pre-commit: lint"
pnpm lint
